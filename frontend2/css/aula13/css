:root {
    /*pra usar variavel yay*/
    --cor: #000
}

* {
    padding: 0;
    margin: 0;
    box-sizing: border-box;
    font-family: Verdana, Geneva, Tahoma, sans-serif;
    background: var(--cor);
}

.apresentacao {
    color: #f34dca;
    text-align: center;
}
