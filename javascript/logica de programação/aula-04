<!DOCTYPE html>
<html>

<head>
    <title>arrays</title>
</head>

<body>
    <p>array é uma forma de armazenar vários valores dentro de uma variável</p>
    <script>
        var mains = ['dva', 'reyna', 'viper'];
        console.log(mains[0]);

        /*var mains = new Array('yuumi', 'xayah', 'lux', 'morgana')
        console.log(mains[0]);*/

        var roles = ['tanque', 'duelista', 'controlador'];
        roles[0] = 'kinda dps ;)'; //muda só o valor da variável 0
        console.log(roles[0]);
        mains.push('sage'); //push: empurra mais uma variável na array
        console.log(mains);

    </script>
</body>

</html>
